To run testapp.py from github linked csci375 Dir


    Activate VE:
        source venv/bin/activate

    install requirements:
        pip install -r requirements.txt

    run:
        python3 ./app.py



IF YOU HAVEA ISSUES

    1. delete venv folder from device
    2. navigate to csci375 folder
    3. run python3 -m venv venv
    4. repeat first set of instructions

